# Installation
1. At the root of project in cmd write `yarn install`.
2. Add .env file with variables, that was described in config/config.service.ts file

# Running this project
Watch mode - yarn start:dev
Build - yarn build
etc.
